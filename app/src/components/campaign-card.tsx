import Link from "next/link";
import type { Campaign } from "@/lib/campaigns";

export function CampaignCard({ campaign }: { campaign: Campaign }) {
  return <article className="flex h-full flex-col rounded-2xl border border-[#dcd7c9] bg-[#fbfaf5] p-5 shadow-[0_2px_0_rgba(23,54,36,0.03)] transition hover:-translate-y-0.5 hover:border-[#9caf90] hover:shadow-md"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-medium text-[#74502d]">{campaign.workType}</p><h2 className="mt-1 text-xl font-semibold tracking-tight text-[#173624]">{campaign.title}</h2><p className="mt-1 text-sm text-[#526257]">{campaign.farm}</p></div><span className="shrink-0 rounded-full bg-[#edf3e7] px-2.5 py-1 text-xs font-semibold text-[#28533b]">{campaign.rating.toFixed(1)} ★</span></div><dl className="mt-5 grid grid-cols-2 gap-x-4 gap-y-3 text-sm"><div><dt className="text-[#66756c]">Zona</dt><dd className="mt-0.5 font-medium text-[#25372b]">{campaign.district}</dd></div><div><dt className="text-[#66756c]">Inicio</dt><dd className="mt-0.5 font-medium text-[#25372b]">{campaign.startDate}</dd></div><div><dt className="text-[#66756c]">Pago</dt><dd className="mt-0.5 font-medium text-[#25372b]">{campaign.paymentDetail}</dd></div><div><dt className="text-[#66756c]">Condiciones</dt><dd className="mt-0.5 font-medium text-[#25372b]">{campaign.includesLodging ? "Incluye alojamiento" : "Sin alojamiento"}</dd></div></dl><div className="mt-5 flex flex-wrap gap-2 text-xs font-medium">{campaign.includesFood && <span className="rounded-full bg-[#f4ead9] px-2.5 py-1 text-[#74502d]">Incluye comida</span>}<span className="rounded-full bg-[#eef1ec] px-2.5 py-1 text-[#405246]">{campaign.workersNeeded} cupos</span></div><Link href={`/campanas/${campaign.slug}`} className="mt-6 inline-flex min-h-11 items-center justify-center rounded-xl border border-[#28533b] px-4 text-sm font-semibold text-[#28533b] transition hover:bg-[#28533b] hover:text-white">Ver campaña</Link></article>;
}
